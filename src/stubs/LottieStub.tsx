import React from 'react';
import { View } from 'react-native';

const LottieView = React.forwardRef(({ style }: any, _ref: any) => <View style={style} />);
LottieView.displayName = 'LottieView';
export default LottieView;
